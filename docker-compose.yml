services:
  tatamishot:
    build: .
    ports:
      - "8484:8484"
    volumes:
      - /path/to/media:/media:ro
      - ./output:/output
    environment:
      - PLEX_TOKEN=your_token_here
      - PLEX_URL=http://plex:32400
      - OUTPUT_DIR=/output
    restart: unless-stopped
