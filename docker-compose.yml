services:
  tatamishot:
    build: .
    ports:
      - "8484:8484"
    volumes:
      - ${MEDIA_DIR}:/media:ro
      - ./output:/output
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
